{% include 'header' %}

#import <UIKit/UIKit.h>
#import "{{ prefix }}{{ module_info.name }}ViewInput.h"
#import "{{ prefix }}{{ module_info.name }}DataDisplayManager.h"

@protocol {{ prefix }}{{ module_info.name }}ViewOutput;

@interface {{ prefix }}{{ module_info.name }}ViewController : UIViewController <{{ prefix }}{{ module_info.name }}ViewInput>

@property (strong, nonatomic) id<{{ prefix }}{{ module_info.name }}ViewOutput> output;
@property (strong, nonatomic) {{ prefix }}{{ module_info.name }}DataDisplayManager* dataDisplayManager;

@end
